///enum created to handle different states for switching homepage views or for travelling to new page with bottom bar
enum Bottom_navigation_control {
  ///
  home,

  ///
  rotation,

  ///
  student,

  ///
  chat,

  /// more state
  profile,
}

enum Active_status { active, inactive }

enum ClockInOutStatus { inn, out, stop }

enum DailyJournalViewType { edit, view, add, addDash }

enum DailyJournalRoute { direct, rotation }

enum DrIntractionAction { add, edit, view }

enum CheckoffsRoute { add, edit, view }

enum DailyWeeklyEval { edit, view, add }

enum ProcedureCountStatus { add, edit }